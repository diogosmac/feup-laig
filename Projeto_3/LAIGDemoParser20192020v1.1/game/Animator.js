/**
 * Animator - Class that encapsulates all the animation logic of the microbes
 */
class Animator {
    /**
     * Constructor of the class
     * @param {GameOrchestrator} orchestrator - reference to the game orchestrator 
     */
    constructor(orchestrator) {
        this.orchestrator = orchestrator;
    }


    /**
     * Method that updates the animations, given the time argument
     * @param {float} deltaTime - time difference between this function call and the last one 
     */
    update(deltaTime) {
        
    }
}